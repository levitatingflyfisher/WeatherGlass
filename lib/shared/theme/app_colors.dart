import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Sky — the brand blues (also the seed).
  static const sky500 = Color(0xFF3D82C9);
  static const sky600 = Color(0xFF2E6BAA);
  static const sky700 = Color(0xFF21528A);

  // Mist — a cool, overcast-light neutral ground for app chrome.
  static const mist50 = Color(0xFFF7FAFC);
  static const mist100 = Color(0xFFEFF3F7);
  static const mist200 = Color(0xFFE1E8F0);
  static const mist700 = Color(0xFF566472);
  static const mist900 = Color(0xFF141C26);

  // Night — dark surfaces.
  static const night = Color(0xFF0F151E);
  static const night2 = Color(0xFF18212E);

  // Accent — the sun.
  static const sunGold = Color(0xFFF5B72E);
}
